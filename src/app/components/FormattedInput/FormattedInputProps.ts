export type FormattedInputProps = {
  /**
   * HTMLInputElement id prop.
   */
  id: string;

  /**
   * HTMLInputElement name prop.
   */
  name: string;

  /**
   * HTMLInputElement required prop.
   * Defaults to false.
   */
  required: boolean;

  /**
   * HTMLInputElement className prop.
   * Optional.
   */
  className?: string;
};
